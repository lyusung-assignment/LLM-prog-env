# 이 파일을 .env 로 복사한 뒤 값을 채우세요. .env 는 커밋되지 않습니다.
#   Windows PowerShell:  Copy-Item .env.example .env

# JupyterLab 접속 토큰 (http://localhost:8888 에서 입력)
JUPYTER_TOKEN=llm

# HuggingFace 토큰 - gated 모델(Llama 등) 받을 때만 필요
HF_TOKEN=

# LangChain에서 OpenAI API 쓸 때만 필요
OPENAI_API_KEY=
