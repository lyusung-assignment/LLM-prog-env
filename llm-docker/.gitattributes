# Force LF for files used inside the container, regardless of the cloning OS
# (mixed-in CRLF breaks execution inside the Linux container)
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.pdf binary
